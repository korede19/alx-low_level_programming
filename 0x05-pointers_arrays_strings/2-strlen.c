#include "holberton.h"
/**
* _strlen - returns length of a string
* @s: declaration of *s
* Return: 0
*/
int _strlen(char *s)
(
int c;

for (c = 0; s[c] != 0; c++)
{
_putchar(str[c]);
}
return (c);
}
